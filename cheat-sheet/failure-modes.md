# 数据失效模式 F1-F6（Debug 真机必备）

> 🧠 作者提出的分类法 · 参考 [VLA 数据工程指南 §3.5](../theory/foundation/vla_data_engineering_guide.md)
> **核心规律**：F1-F6 **都不会在训练 Loss 上体现**——只能通过主动对抗性评测发现

---

## 🚨 一张速查表（打印贴墙上）

| 失效类 | 训练 Loss | 仿真 | 真机 | 首选检测方法 | 📎 论文证据 |
|--------|:--------:|:---:|:---:|------------|------------|
| **F1 虚假相关** | 低 | 高 | **崩** | LIBERO-PRO 扰动 | [LIBERO-PRO](https://arxiv.org/abs/2510.03827) 90%→0.0% |
| **F2 OOD 偏移** | 低 | 高 | **下降** | 主动边角测试 | [LIBERO-Plus](https://arxiv.org/abs/2510.13626) 95%→<30% |
| **F3 时序错位** | 正常 | 中 | 慢半拍 | 动作-图像同步可视化 | 🧠 经验值 |
| **F4 坐标漂移** | 正常 | 高 | 新数据后变差 | 分布对比 | 🧠 经验值 |
| **F5 语言解耦** | 低 | 高 | 换指令无反应 | 语言消融 | [LIBERO-Para](https://arxiv.org/abs/2603.28301) 44.6%≈47.8% |
| **F6 动作偏斜** | 低 | 高（无扰动） | 失败后卡死 | 扰动恢复测试 | RoboMIND 2.0 5K 失败数据设计 |

---

## 🔴 F1 · 虚假相关（Spurious Correlation）

### 症状
仿真/同分布完美，**换颜色/背景/位置就崩**

### 根因
模型找到了捷径——"任务 A 永远在桌子左边" → 学到的是**位置**不是**任务**

### 📎 典型实证
**[LIBERO-PRO (arXiv:2510.03827)](https://arxiv.org/abs/2510.03827)**：
- 标准设定：90%+
- 换物体/背景/位置的泛化设定：**0.0%**

### 🧠 检测建议
1. **遮蔽语言**：动作不变 → 忽略语言
2. **遮蔽部分视觉**：成功率不变 → 没用到那部分
3. **换物体颜色**：崩溃 → 学了颜色特征

### 🧠 修复建议
- 采集时**刻意多样化**：初始条件、物体位置、背景纹理
- 引入 **RoboEngine 类背景替换**（📎 +210% vs 无增强）

---

## 🔴 F2 · 分布外偏移（OOD Shift）

### 症状
训练集 → 部署环境差一点点，**性能腰斩**

### 根因
数据覆盖不够——光照、相机位置、桌面纹理、物体尺寸

### 📎 间接实证
**[LIBERO-Plus](https://arxiv.org/abs/2510.13626)**：4 种扰动全部导致 95%→<30%

### 🧠 检测建议
主动构造边角案例（极端光照、罕见位置、陌生物体）测试

### 🧠 修复建议
- **Domain Randomization**（仿真侧）
- **多场景真实数据**（DROID 564 场景 是最佳实践）

---

## 🔴 F3 · 时序错位（Temporal Misalignment）

> 🧠 工程经验分类——但很少有论文专门量化对 VLA 性能的敏感度

### 症状
模型预测动作**总是慢半拍**，抓取时机差一帧

### 根因
图像时戳和动作时戳对齐差 **30-100ms**

### 典型诱因
- USB 相机软件时戳漂移（可达 50ms = 30Hz 视频 1.5 帧错位）
- ROS2 时间同步配置错误
- 硬件触发缺失

### 🧠 检测建议
画图对比连续帧的图像变化方向和 action 方向——应该**同步变化**

### 🧠 修复建议
- **硬件触发**（<0.1ms）
- **PTP (IEEE 1588)** 或 **ROS2 /clock** 同步（5-10ms）
- 采集后用**光学事件**（闪光灯）标定时延

---

## 🔴 F4 · 坐标系/动作漂移（Frame Drift）

> 🧠 工程经验——常见痛点

### 症状
**同一个模型，上周跑得好，这周不行**

### 根因
- 相机位置变了没重新手眼标定
- 机器人零位漂移
- 新批次采集员坐标系定义不同（比如 EE 原点放在末端 vs 工具中心）

### 🧠 检测建议
对比新旧数据的 EE 轨迹统计分布——均值/方差差异过大 = 漂移

### 🧠 修复建议
- 每次重装相机**必须**重手眼标定
- 元数据里记录变换矩阵（`T_base_to_camera` / `T_base_to_world`）
- 按批次分 chunk 训练，避免多批次混训

---

## 🔴 F5 · 语言-动作解耦（Language-Action Decoupling）

### 症状
**换任务描述，模型行为不变**

### 根因
语言标注是模板化的（`pick up the {obj}`）→ 视觉已经有足够信息识别任务 → 语言成摆设

### 📎 论文证据
- **[LIBERO-PRO](https://arxiv.org/abs/2510.03827)**：做了语言消融
- **[LIBERO-Para (arXiv:2603.28301)](https://arxiv.org/abs/2603.28301)**：
  - vision-only: **44.6%**
  - language-conditioned: **47.8%**
  - **含义**：VLA 完全忽略语言

### 检测方法（黄金标准）
**语言消融** —— mask 语言看成功率是否下降

### 🧠 修复建议
1. 用 **LLM 重写**同一任务的多种表述：
   - "pick up the red block"
   - "grab the crimson cube"
   - "move the scarlet piece"
2. 让视觉**不能单独识别任务**——同一场景布置多个物体，必须靠语言消歧
3. 引入需要**世界知识**的指令（"pour a small amount"——"small" 不是视觉可见的）

---

## 🔴 F6 · 动作分布偏斜（Action Distribution Bias）

> 🧠 作者归纳 · 间接证据：RoboMIND 2.0 的 5K 失败案例设计动机

### 症状
模型在常见动作上表现好，**罕见动作（回退、重试、微调）完全不会**

### 根因
只收集"完美 demo" → 模型没见过"失败后怎么恢复"

### 典型现象
抓取失败后模型**卡住不动**（不会重试）

### 🧠 检测建议
部署时**人为制造偏离态**（挪一下物体）→ 看模型能否自动纠正

### 🧠 修复建议
1. **保留失败 + 恢复轨迹**（📎 RoboMIND 2.0 的 5K 失败案例专门应对此问题）
2. **DAgger 类数据增强**：部署时收集"模型跑歪 + 人工救场"的混合数据
3. **动作扰动**：训练时随机扰动专家动作然后学"纠正回来"

---

## 🎯 诊断决策树（真机不 work 时的第一步）

```
真机成功率异常
    │
    ├─ 训练 Loss 是否正常？
    │      │
    │      ├─ 不正常 → 数据有根本问题（格式/采集错）
    │      └─ Loss 低（拟合好）
    │             │
    │             ├─ 同分布正常、换条件崩？
    │             │   ├─ 换物体/背景 → 【F1 虚假相关】
    │             │   ├─ 换环境/光照 → 【F2 OOD 偏移】
    │             │   ├─ 换指令无反应 → 【F5 语言解耦】
    │             │   └─ 失败后卡死 → 【F6 动作偏斜】
    │             │
    │             └─ 每次都差点意思（偏差稳定）
    │                   │
    │                   ├─ 动作总是慢半拍 → 【F3 时序错位】
    │                   └─ 新数据后变差 → 【F4 坐标漂移】
    ▼
 定位失效类 → 对应小节的"修复建议"
```

---

## 💡 Debug 工作流（建议步骤）

### Step 1: 看 loss 曲线
- **训练 loss 正常 + 真机坏** → F3 / F4（时序 / 坐标）先查
- **训练 loss 低 + 仿真高 + 真机崩** → F1 / F2 / F5 / F6

### Step 2: 做 4 个快速测试（各 30 分钟）
1. **语言消融**：mask 语言 → 对比成功率（→ F5）
2. **位置扰动**：移动物体 10cm → 对比成功率（→ F1）
3. **环境扰动**：换背景 → 对比成功率（→ F2）
4. **失败恢复**：部署时挪动物体 → 看能否恢复（→ F6）

### Step 3: 如果 loss 曲线异常
- **训练 loss 不下降** → 数据预处理 bug（查 normalization / tokenization）
- **训练 loss 下降但仿真也崩** → 标注本身错（查 action alignment）
- **验证 loss 不下降** → 过拟合 + 数据多样性不够（加增强）

---

## 📚 延伸阅读

- [VLA 数据工程指南 §3.5](../theory/foundation/vla_data_engineering_guide.md) · 6 种失效模式详细论文证据
- [Benchmarks + 警告](./benchmarks.md) · LIBERO-PRO / Para / Plus / X 原始数据
- [核心公式 §7 归一化](./formulas.md) · 避免 F4 的数学基础

---

[← Back to Cheat Sheet](./README.md)
